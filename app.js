let chart,searchTimer;const names={ema:'EMA 20/50',rsi:'RSI 14',macd:'MACD',bollinger:'Bollinger Bands',breakout:'20-daagse Breakout'};const rules={ema:['EMA 20 kruist boven EMA 50 = koop','EMA 20 kruist onder EMA 50 = verkoop'],rsi:['RSI komt terug boven 30 = koop','RSI zakt terug onder 70 = verkoop'],macd:['MACD kruist boven signaallijn = koop','MACD kruist onder signaallijn = verkoop'],bollinger:['Koers herstelt boven onderste band = koop','Koers zakt onder middenband = verkoop'],breakout:['Slot boven hoogste koers van 20 perioden = koop','Slot onder laagste koers van 10 perioden = verkoop']};function val(id,v){document.getElementById(id).textContent=v}function cls(id,n){const e=document.getElementById(id);e.className=n>=0?'good':'bad'}function fmt(n,d=2){n=Number(n);return Number.isFinite(n)?n.toLocaleString('nl-NL',{maximumFractionDigits:d}):'—'}function setRules(s){val('activeStrategy',names[s]);document.getElementById('rules').innerHTML=rules[s].map(x=>`<span>${x}</span>`).join('')}async function liveSearch(){const input=document.getElementById('symbol'),q=input.value.trim(),box=document.getElementById('searchResults'),state=document.getElementById('searchState');clearTimeout(searchTimer);if(q.length<2){box.classList.add('hidden');state.classList.add('hidden');return}state.classList.remove('hidden');box.classList.add('hidden');searchTimer=setTimeout(async()=>{try{const r=await fetch(`/api/search?q=${encodeURIComponent(q)}`),d=await r.json();state.classList.add('hidden');if(!r.ok)throw new Error(d.error||'Zoeken mislukt');box.innerHTML=d.length?d.map(x=>`<div class="result" onclick="chooseStock('${String(x.symbol).replaceAll("'",'')}')"><span class="sym">${x.symbol||''}</span><div><b>${x.instrument_name||x.name||''}</b><br><small>${x.exchange||''}</small></div><small>${x.country||x.currency||''}</small></div>`).join(''):'<div class="empty">Geen aandelen gevonden.</div>';box.classList.remove('hidden')}catch(e){state.classList.add('hidden');box.innerHTML=`<div class="empty">${e.message}</div>`;box.classList.remove('hidden')}},300)}function chooseStock(s){document.getElementById('symbol').value=s;document.getElementById('searchResults').classList.add('hidden');loadStock()}async function loadStock(){const symbol=document.getElementById('symbol').value.trim().toUpperCase(),strategy=document.getElementById('strategy').value,interval=document.getElementById('interval').value;if(!symbol)return;document.getElementById('searchResults').classList.add('hidden');val('status','LADEN...');setRules(strategy);try{const r=await fetch(`/api/analyze?symbol=${encodeURIComponent(symbol)}&strategy=${strategy}&interval=${interval}`),d=await r.json();if(!r.ok)throw new Error(d.error||'Fout');const q=d.quote||{},b=d.backtest,last=d.points.at(-1)||{};val('ticker',q.symbol||symbol);val('name',q.name||d.meta?.symbol||'');val('market',[q.exchange,d.meta?.currency].filter(Boolean).join(' · ')||'—');const p=Number(q.close||q.price||last.close);val('price',fmt(p,4));const ch=Number(q.percent_change||0);val('change',`${ch>=0?'+':''}${ch.toFixed(2)}%`);cls('change',ch);val('winrate',b.winrate.toFixed(1)+'%');val('return',(b.total_return>=0?'+':'')+b.total_return.toFixed(2)+'%');cls('return',b.total_return);val('trades',b.trades);val('pf',b.profit_factor);val('dd',b.max_drawdown.toFixed(2)+'%');cls('dd',b.max_drawdown);val('strategyName',names[strategy]);val('open',fmt(q.open||last.open,4));val('high',fmt(q.high||last.high,4));val('low',fmt(q.low||last.low,4));val('volume',fmt(q.volume||last.volume,0));val('status',q.is_market_open===true?'● MARKT OPEN':'MARKTDATA');draw(d.points,strategy)}catch(e){val('status','FOUT');alert(e.message)}}function draw(points,strategy){const intraday=document.getElementById('interval').value!=='1day';const labels=points.map(p=>{const d=new Date(p.time);return intraday?d.toLocaleString('nl-NL',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}):d.toLocaleDateString('nl-NL',{day:'2-digit',month:'short',year:'2-digit'})});const datasets=[{label:'Koers',data:points.map(p=>p.close),borderColor:'#edf4ff',borderWidth:1.8,pointRadius:0,tension:.08}];if(strategy==='ema')datasets.push({label:'EMA 20',data:points.map(p=>p.ema20),borderColor:'#ff8b32',borderWidth:1.3,pointRadius:0},{label:'EMA 50',data:points.map(p=>p.ema50),borderColor:'#2396ff',borderWidth:1.3,pointRadius:0});if(strategy==='bollinger')datasets.push({label:'BB boven',data:points.map(p=>p.bb_upper),borderColor:'#2396ff',borderWidth:1,pointRadius:0},{label:'BB midden',data:points.map(p=>p.bb_mid),borderColor:'#7d8da5',borderWidth:1,pointRadius:0},{label:'BB onder',data:points.map(p=>p.bb_lower),borderColor:'#2396ff',borderWidth:1,pointRadius:0});datasets.push({type:'scatter',label:'Koop',data:points.map((p,i)=>p.buy?{x:labels[i],y:p.close}:null).filter(Boolean),backgroundColor:'#42daa1',pointRadius:5},{type:'scatter',label:'Verkoop',data:points.map((p,i)=>p.sell?{x:labels[i],y:p.close}:null).filter(Boolean),backgroundColor:'#ff687b',pointRadius:5});if(chart)chart.destroy();chart=new Chart(document.getElementById('chart'),{type:'line',data:{labels,datasets},options:{responsive:true,maintainAspectRatio:false,interaction:{mode:'index',intersect:false},plugins:{legend:{position:'top',align:'start',labels:{color:'#9fb0c8',boxWidth:18}}},scales:{x:{ticks:{color:'#667b96',maxTicksLimit:9},grid:{color:'#14263a'}},y:{position:'right',ticks:{color:'#8194af'},grid:{color:'#14263a'}}}}})}async function compareStrategies(){const symbol=document.getElementById('symbol').value.trim().toUpperCase(),interval=document.getElementById('interval').value;if(!symbol)return;val('status','VERGELIJKEN...');try{const r=await fetch(`/api/compare?symbol=${encodeURIComponent(symbol)}&interval=${interval}`),d=await r.json();if(!r.ok)throw new Error(d.error||'Fout');document.getElementById('compareBody').innerHTML=d.map(x=>`<tr><td><b>${names[x.strategy]}</b></td><td>${x.winrate}%</td><td class="${x.total_return>=0?'good':'bad'}">${x.total_return>=0?'+':''}${x.total_return}%</td><td>${x.trades}</td><td>${x.profit_factor}</td><td class="bad">${x.max_drawdown}%</td><td><button class="selectbtn" onclick="selectStrategy('${x.strategy}')">Selecteer</button></td></tr>`).join('');val('status','MARKTDATA')}catch(e){val('status','FOUT');alert(e.message)}}function selectStrategy(s){document.getElementById('strategy').value=s;loadStock();window.scrollTo({top:0,behavior:'smooth'})}const symbolInput=document.getElementById('symbol');symbolInput.addEventListener('input',liveSearch);symbolInput.addEventListener('keydown',e=>{if(e.key==='Enter')loadStock()});document.addEventListener('click',e=>{if(!e.target.closest('.searchwrap'))document.getElementById('searchResults').classList.add('hidden')});